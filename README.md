> (以`键：值 / key: value`代指本项目的替换对象)

**通用处理**：
- 对应如果有重复的，目前一般以已有的最多的一个为准
- 冒号多于一个的判断为可能是描述文本，跳过

**对于键**：
- 目前主要以手动筛选为主

**对于值**：
- 只处理键在白名单里的（以防有些描述文本里也带有引号或者其他奇怪的edge cases）
- 厂商名统一为：`中文名+空格+英文括号(英文名)`
- 明确为各种数值的，使用模板进行翻译
- 食物/饮品的食用效果进行直接替换
- 其他暂时保留为原文